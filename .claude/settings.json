{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/sync-copilot-to-claude.py\"; [ -f \"$S\" ] || exit 0; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/workspace-doctor.py\"; [ -f \"$S\" ] || exit 0; exec python3 \"$S\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/intent-gate.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/intent-gate.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/branch-guard.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/branch-guard.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-edit-path.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-edit-path.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-prod-gate.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-prod-gate.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-commit-gate.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-commit-gate.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-push-gate.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-push-gate.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-branch-gate.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-branch-gate.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-signin-gate.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-signin-gate.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-issue-gate.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-issue-gate.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-merge-gate.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-merge-gate.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/intent-gate.py\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/intent-gate.py is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec python3 \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-reminders.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-reminders.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          },
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/pretooluse-edit-path.sh\"; [ -f \"$S\" ] || { printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"HARNESS GUARD DOWN: the hook script $HOME/PangeaChat/.github/scripts/pretooluse-edit-path.sh is registered but missing, so this command ran UNCHECKED. Nothing you did caused it \\u2014 the script lives in a repo checkout, and a branch switch or an interrupted sync can move it out from under an absolute path. Restore it (`git -C ~/PangeaChat/.github checkout origin/main -- <path>`) and report it with the report-process-breach skill. Do not edit settings to route around a guard.\"}}'; exit 0; }; exec bash \"$S\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/stop-ops-direct-check.py\"; [ -f \"$S\" ] || exit 0; exec python3 \"$S\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "S=\"$HOME/PangeaChat/.github/scripts/sync-copilot-on-edit.sh\"; [ -f \"$S\" ] || exit 0; exec bash \"$S\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if jq -r '.tool_input.command // empty' 2>/dev/null | grep -qE '\\bgh pr merge\\b'; then \"$HOME/PangeaChat/.github/scripts/sweep-worktrees.sh\" --emit-claude; fi"
          }
        ]
      }
    ]
  }
}
